import * as React from "react";
import SvgIcon from "@material-ui/core/SvgIcon";

const SvgControlsStop = (props) =>
  React.createElement(
    SvgIcon,
    props,
    <rect x={2} y={2} width={20} height={20} rx={1.5} ry={1.5} />
  );

export default SvgControlsStop;
